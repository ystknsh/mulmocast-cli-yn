import "dotenv/config";
export declare const createMulmoScriptWithInteractive: ({ outdir, filename, template_name }: {
    outdir: string;
    filename: string;
    template_name?: string;
}) => Promise<void>;
